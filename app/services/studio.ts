import { STUDIO } from '../studio';

/**
 * Who made the music: the engine, the same name the server writes into the
 * file's artist tag, so the app and the file say the same thing.
 */
export const TRACK_ARTIST = STUDIO.artist;
